<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - MathWise</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@400;700&display=swap" rel="stylesheet"> 
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.0/font/bootstrap-icons.min.css" rel="stylesheet"> 
    <style>
        body {
            font-family: 'Baloo 2', cursive;
            background-color: #89D6F8;
        }
        .card {
            max-width: 700px;
            width: 100%;
            position: relative;
            background: url('/shape.png') no-repeat center center;
            background-size: cover;
        }
        .logo {
            position: absolute;
            top: 15px;
            right: 15px;
            width: 150px; 
            height: 150px; 
        }
        .btn-primary {
            background-color: #6773FF;
            border-color: #6773FF;
        }
    </style>
</head>
<body>

    <div class="container-fluid min-vh-100 d-flex justify-content-center align-items-center">
        <div class="card shadow-lg">
            <img src="/assets/logo.png" alt="Logo" class="logo">

            <div class="card-body p-5">
                <h1 class="card-title mb-3" style="color: #680A76; font-weight: bold;">Forgot Password</h1>

                <p class="card-text text-left" style="color: #680A76; font-size: 1rem; line-height: 1.5;">
                    Enter your email address and we'll <br>
                    send you a new password and <br>
                    confirmation.
                </p>

                <% if (typeof successMessage !== 'undefined' && successMessage.length > 0) { %>
                    <div class="alert alert-success" id="success-alert">
                        <%= successMessage[0] %>
                    </div>
                <% } %>
                <% if (typeof error !== 'undefined' && error.length > 0) { %>
                    <div class="alert alert-danger" id="error-alert">
                        <%= error[0] %>
                    </div>
                <% } %>

                <form action="/mail/forgetpassword" method="POST">
                    <div class="form-group mb-3">
                        <input type="email" id="email" name="email" class="form-control" placeholder="Email" required>
                    </div>
                    <button type="submit" class="btn btn-primary w-100">Send Reset Link</button>
                </form>

                <div class="text-center mt-3">
                    <a href="/login" class="text-muted">Back to Login</a>
                </div>

                <div class="position-absolute bottom-0 start-0 p-3"> 
                    <p class="text-muted mb-0">Powered by <strong>MathWise</strong></p>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        window.onload = function() {
            var successAlert = document.getElementById('success-alert');
            if (successAlert) {
                setTimeout(function() {
                    successAlert.style.display = 'none'; // Hide the alert
                }, 3000);
            }

            var errorAlert = document.getElementById('error-alert');
            if (errorAlert) {
                setTimeout(function() {
                    errorAlert.style.display = 'none'; // Hide the alert
                }, 3000);
            }
        }
    </script>
</body>
</html>
